[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: tests requiring PostgreSQL (use -m integration)
    redis: tests requiring Redis (use -m redis)
    agent: tests for the agent package
    lifecycle: tests requiring full application lifecycle (startup/shutdown)
